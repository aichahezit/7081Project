<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bcit.aicha.hezit.a7081project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activites.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activites.ViewItemActivity" android:parentActivityName=".Activites.MainActivity" />
        <activity android:name=".Activites.ViewListActivity" android:parentActivityName=".Activites.MainActivity"/>
        <activity android:name=".Activites.EnterItemActivity" android:parentActivityName=".Activites.MainActivity"></activity>
    </application>

</manifest>