<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:background="#EBF5FB"
    tools:context="bcit.aicha.hezit.a7081project.Activites.MainActivity">

    <Button
        android:text="Patient Info"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/patientBtn"
        android:background="@drawable/round_button"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="patientView" />

    <Button
        android:text="Visits"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/visitBtn"
        android:background="@drawable/round_button"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/doctorBtn"
        android:onClick="visitView" />

    <Button
        android:text="Doctors"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/doctorBtn"
        android:background="@drawable/round_button"
        android:textAlignment="center"
        android:elevation="1dp"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/patientBtn"
        android:onClick="doctorView" />
</RelativeLayout>
